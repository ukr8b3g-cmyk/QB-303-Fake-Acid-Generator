{
  "name": "qb-303-fake-acid-generator",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny browser acid toy with squelchy bass, drums and blinking LEDs.",
  "scripts": {
    "test": "node --test tests/core.test.cjs",
    "check": "node --check engine.js && node --check app.js"
  }
}
